==============================================================
 데이터셋·전처리 정리 (팀 공유용)
 과제: ⑤ 제조 생산데이터 기반 전력사용량 예측 및 최대피크 위험조건 분석
 기준일: 2026-09-23
 범위: 파일별 역할 / 기존 전처리 점검 / 보강 내용과 검증 / 데이터 특성 /
       상관관계와 예측 가능성의 근거 / 모델 코드 설명 / 1차 비교 실험 결과 /
       15분 컬럼 두 해석 비교 / 1주 앞 예측
 관련 문서: 작업내역.txt (결정사항 D01 to D16 · 팀원 관리), 피처목록.txt (피처 우선순위)
==============================================================

■ 세 줄 요약
  1. 팀원이 만든 전처리(preprocess.py)는 구조가 탄탄하다. 복제일·휴무일·가동중단
     판별과 "load() 단일 입구" 설계는 그대로 간다.
  2. 보강한 핵심은 두 가지다.
     ① 생산 기록 누락일을 2일 → 15일로 넓혀 탐지 (train_ok_strict)
     ② 당일 휴무 플래그(is_off)를 달력 전용 피처 세트에서 뺌 (누수성 모순 제거)
  3. 그 결과 성능 수치가 바뀌었다. 생산계획을 받는 시나리오는 MAE 11.13 → 10.35로
     좋아졌고, 달력만 쓰는 시나리오는 13.36 → 21.56으로 "나빠졌다".
     후자는 성능 하락이 아니라 원래 부풀려져 있던 값이 제자리를 찾은 것이다.


==============================================================
 [1] 파일이 각각 왜 존재하는가
==============================================================

  ▷ 데이터
    dataset/okm_augumented_2021.csv
        원본. 2021-01-01 to 09-14, 시간 단위 6,168행(257일 x 24시간), 18개 컬럼.
        절대 수정하지 않는다. 모든 가공은 코드로만 한다(재현성 배점 10점).

  ▷ 코드 (src/) — 실행 순서대로
    preprocess.py   ★ 모든 단계의 단일 입구.
        원본을 읽어 정제하고 "이 행을 학습에 써도 되는가"를 판단하는 플래그를 붙인다.
        다른 스크립트는 전부 여기의 load() 를 호출한다. 전처리 규칙이 한 곳에만
        있어야 분석·학습·평가가 서로 어긋나지 않기 때문이다.

    eda.py          EDA 그림 7장 생성 → outputs/eda/01 to 07.png
        모든 그림을 복제일 / 고유일로 나눠 그린다. 섞어 그리면 관계가 왜곡된다.

    eda3.py         기저부하 구조와 피크 조건을 숫자로 출력 (그림 없이 콘솔)
        "생산량 0인데 전력을 쓰는 시간"을 파고드는 용도.

    pattern.py      날짜별 24시간 전력곡선에 패턴 ID를 부여 → outputs/daily_pattern.csv
        같은 곡선이 몇 번 반복되는지 = 증강 복제의 규모를 재는 도구.

    baseline.py     베이스라인 4종을 시간순 롤링 폴드로 평가 → outputs/baseline_results.csv
        모델이 넘어야 할 기준선. 이걸 못 이기면 모델을 쓸 이유가 없다.

    features.py     모델 입력 피처 생성 → outputs/features.csv, outputs/peak_ratio.csv
        피처목록.txt 의 A to D 분류를 코드로 옮긴 것. 예측 시점에 알 수 있는
        정보만 쓰도록 lag 24 이상만 참조한다.

    model.py        예측 모델 학습·비교 → outputs/model_results.csv
        LightGBM / RandomForest 를 시간순 롤링 폴드로 평가한다. 자세한 설명은 [8].

    forecast.py     특정 날짜 하루를 1주 앞에서 예측 → outputs/forecast_{날짜}.csv
        제출물 "테스트데이터 예측결과 파일"을 만드는 자리다. 자세한 설명은 [11].

    __init__.py     src 를 패키지로 인식시키기 위한 빈 파일.

  ▷ 실행·환경
    run_all.py      위 6단계를 순서대로 실행. 어느 폴더에서 실행해도 경로가 맞는다.
    environment.yml conda 환경 정의.
    README.md       폴더 구조·실행 방법.

  ▷ 산출물 (outputs/)
    eda/01 to 07.png        EDA 그림
    eda/captions.txt        그림 캡션
    eda/clean_preview.csv   전처리 결과 전체 (플래그 포함). 눈으로 확인하는 용도
    daily_pattern.csv       날짜별 패턴 ID
    baseline_results.csv    베이스라인 성적표
    features.csv            모델 입력 피처 (메타 열 + 피처 열)
    peak_ratio.csv          시각별 (15분 최대 / 시간 평균) 환산 계수
    model_results.csv       모델 비교 실험 성적표 (설정 x 시드 x 폴드)
    forecast_{날짜}.csv     특정 날짜 24시간 예측 (시간평균 + 15분최대 환산)

  ▷ 문서
    작업내역.txt          프로젝트 현황과 결정사항 D01 to D16. 가장 먼저 읽을 문서 (팀원 관리)
    피처목록.txt          피처 후보와 우선순위, 구현된 열 이름 (팀원 관리, 옛 상태)
    작업내역(조선제).txt  이 문서
    code_artifact.txt     과제 원문


==============================================================
 [2] 팀원이 이미 해놓은 전처리 (그대로 유지)
==============================================================
  모두 preprocess.py 의 load() 안에 있다.

  처리                  내용                                        근거
  --------------------  ------------------------------------------  ------------
  시간 컬럼 복구        7/13, 7/15 의 시간값이 70 to 188로 손상 →     D04
                        날짜별 행 순서로 0 to 23 재부여
  결측 보간             풍속 3행·강수량 1행 선형보간,               D05
                        공장인원 17행은 0
  가동 중단 판별        15분 값 중 하나라도 0인 시간 = is_stop       D08
                        (8/28 17시 to 8/29 11시 + 9/8 12시, 20행)
  휴무일 판별           일 최대전력 30 미만 = is_off (49일)          D09
  기록 손상일 판별      7/13, 7/15 = is_corrupt (48행)               D14
  복제일 판별           24시간 전력곡선이 다른 날과 완전히 같은 날    D06
                        = is_clone (161일, 45개 그룹)
  학습 가능 행          train_ok = is_stop 도 is_corrupt 도 아닌 행  D08/D14
                        (6,100행)

  ▷ 이 설계에서 특히 잘 된 점
    - load() 단일 입구: 전처리 규칙이 한 곳에만 있어 분석과 학습이 어긋나지 않는다.
    - 복제일 분리: 증강 데이터를 걸러내지 않으면 성능이 2배 좋게 보인다.
      (lag168 베이스라인 MAE 복제일 11.6 vs 고유일 23.6)
    - lag 입력에서 휴무·중단 값을 결측 처리(D11): 휴가 다음 주 lag168 오염을 막는다.


==============================================================
 [3] 보강한 내용 ★
==============================================================

  ----------------------------------------------------------------
  보강 1. 생산 기록 누락일 탐지를 2일 → 15일로 확대   [preprocess.py]
  ----------------------------------------------------------------
  문제
      기존에는 7/13, 7/15 만 제외 대상이었다(is_corrupt). 그런데 이 이틀은
      "시간 컬럼까지 같이 손상돼서" 우연히 눈에 띈 것이지, 생산 기록 누락의
      전체 목록이 아니다.
  발견
      "하루 종일 생산량이 0인데 일 최대전력이 기저부하를 한참 넘는 날"을
      기준으로 다시 세면 15일이 나온다 (복제일 9 / 고유일 6).
        20210101 20210102 20210110 20210124 20210211 20210212 20210213
        20210301 20210307 20210314 20210321 20210328 20210509 20210713 20210715
      예) 3/21(일) 생산량 0인데 일 최대 190, 3/28(일) 생산량 0인데 185.
      7/13, 7/15 와 성격이 같은데 시간 컬럼이 멀쩡해서 안 걸렸던 날들이다.
  왜 문제인가
      이런 행을 그대로 학습하면 모델이 "생산 0인데 고부하"를 배워서
      생산량 피처의 기울기가 눌린다.
  처리
      is_prod_missing 플래그 추가.
      train_ok_strict = train_ok & ~is_prod_missing  (6,100행 → 5,788행)
      ※ 기존 train_ok 는 건드리지 않았다. 기존 결과와 비교할 수 있어야 하고,
        무엇을 제외할지는 팀이 정할 문제이기 때문이다.
  효과 (측정값은 [4] 참고)
      생산량-전력 스피어만 상관   0.825 → 0.862
      LightGBM MAE (full 세트)   11.10 → 10.35

  ----------------------------------------------------------------
  보강 2. is_off 를 달력 세트에서 제거 (누수성 모순)   [features.py]
  ----------------------------------------------------------------
  문제
      is_off 는 "일 최대전력 30 미만"으로 정의된다. 즉 타깃에서 유도한 값이라
      예측 시점에는 알 수 없다. 팀 합의는 "생산계획으로 알 수 있다고 가정"(D09)인데,
      그렇다면 is_off 는 생산계획을 받는 시나리오에서만 쓸 수 있다.
      그런데 기존 코드에서 is_off 는 A군(달력)에 들어 있었고,
      A군은 "생산계획이 없는 시나리오"용 세트(no_plan)에 그대로 포함된다.
      → 생산계획 없이 쓰는 세트가 생산계획이 있어야 아는 값을 쓰고 있었다.
  처리
      is_off 를 A군 → B군(생산계획)으로 이동.
      단, after_off / off_run_prev / days_since_off 는 "지난 휴무 이력"이라
      전날 실적으로 알 수 있으므로 A군에 남긴다. 과거를 보는 값과
      당일을 보는 값을 가른 것이 핵심이다.
  효과
      no_plan 세트 MAE 13.36 → 25.32.
      ★ 이건 성능이 나빠진 게 아니라, 13.36 이라는 숫자가 애초에
        타깃 유래 정보로 부풀려져 있었다는 뜻이다. 25.32 가 실체다.
        팀이 "달력만 있어도 MAE 13 나온다"고 믿고 있으면 오판이 된다.

  ----------------------------------------------------------------
  보강 3. 휴무 이력 피처 3종 추가   [features.py]
  ----------------------------------------------------------------
  근거
      기존 after_off 는 0/1 이라 "어제 하루 쉬었다"와 "9일 하계휴가 뒤 복귀"를
      구분하지 못한다. 실제로 복귀일 부하는 다르다.
        직전 휴무 0일(평상시) 일 최대 평균 165.2
        직전 휴무 1일          182.2
        직전 휴무 9일(하계휴가) 193.0
  추가 피처
      off_run_prev     직전 연속 휴무 길이 (하계휴가 복귀일이면 9)
      days_since_off   마지막 휴무 이후 경과일
      is_holiday       법정공휴일 여부 (2021 달력, 대체공휴일 포함)
  효과
      no_plan 세트 MAE 25.32 → 23.19 (-8.4%)
      full 세트에서는 기여가 작다. 생산계획이 있으면 그쪽이 더 강하기 때문.
      ※ 9일 휴가는 학습 구간에 1번뿐이라 통계적 근거는 약하다. 표본이 늘면 재확인할 것.

  ----------------------------------------------------------------
  보강 4. day_prod_zero — is_off 의 누수 없는 대체값   [features.py]
  ----------------------------------------------------------------
  발견
      휴무일 49일은 100% 전부 일 생산량이 0이다 (포함관계 확인).
      → 생산계획만 받으면 휴무일 후보를 타깃 없이 만들 수 있다.
      단 역은 성립하지 않는다. 일 생산량 0인 64일 중 15일은 고부하다
      (= 보강 1의 생산 기록 누락 의심일). 일치율 93.75%,
      보강 1로 그 15일을 빼면 일치율이 100%가 된다.

  ----------------------------------------------------------------
  보강 5. 공휴일 실태 확인 — 이 공장은 법정공휴일에 쉬지 않는다   [preprocess.py]
  ----------------------------------------------------------------
  확인 결과 (학습 구간 내 공휴일 10일)
      날짜        공휴일        휴무?   일최대   일생산량
      2021-01-01  신정          가동     107         0
      2021-02-11  설연휴        가동     168         0   (복제일)
      2021-02-12  설날          가동     109         0   (복제일)
      2021-02-13  설연휴        가동     109         0   (복제일)
      2021-03-01  삼일절        가동     179         0   (복제일)
      2021-05-05  어린이날      가동     112     2,190
      2021-05-19  석가탄신일    가동      97     2,990
      2021-06-06  현충일        휴무      25         0   ← 일요일
      2021-08-15  광복절        휴무      23         0   ← 일요일
      2021-08-16  대체공휴일    가동     183    10,238  ← 연중 최대급 생산
  해석
      실제로 쉰 공휴일 2일은 둘 다 일요일이다. 즉 "공휴일이라서" 쉰 날은 0일이다.
      5/5, 5/19, 8/16 은 생산량까지 많은 정상 가동일이다.
  시사점 ★
      테스트 구간에 추석(9/20 to 22)이 포함되더라도 이 공장은 가동할 가능성이 높다.
      작업내역 D09 의 "추석이 포함될 가능성 → 모델이 휴무를 배워야 함"은
      전제를 다시 볼 필요가 있다. 실제 조업 중단은 하계휴가(7/31 to 8/8)처럼
      회사 자체 휴무로만 나타난다.
      is_holiday 를 피처로 넣어는 뒀지만 기여도는 낮다(전체 36개 중 30위).
      주최 측이 추석 휴무 여부를 알려주면 피처가 아니라 is_off 를 직접
      지정하는 쪽이 안전하다.

  ----------------------------------------------------------------
  보강 6. 데이터 무결성 검증 추가   [preprocess.py]
  ----------------------------------------------------------------
      lag 피처는 shift(24), shift(168) 처럼 "1행 = 1시간, 누락 없이 연속"을
      전제한다. 이 전제가 깨지면 예외가 아니라 조용히 틀린 lag 가 만들어진다.
      → 하루 24행인지, dt 간격이 1시간인지 assert 로 막았다.
      테스트 데이터를 이어 붙일 때 특히 중요하다.

  ----------------------------------------------------------------
  보강 7. 피크 환산 계수 산출 → outputs/peak_ratio.csv   [features.py]
  ----------------------------------------------------------------
      시각별 (15분 최대 / 시간 평균) 평균 비율.
        7시 1.30 · 17시 1.17 · 5시 1.15 · 12시 1.15 · 나머지 약 1.05
      교대 전환 시각에 값이 크다. 시간 평균을 예측하고 이 계수로 곱해
      요금 기준 피크(15분 최대)를 환산하는 2단계 구조에 쓴다.
      서브 개발자의 피크 분석·스케줄링 입력이다.

  ----------------------------------------------------------------
  누수 재검증
  ----------------------------------------------------------------
      8/30(가동일) 하루의 전력값을 999로 바꾸고 피처를 다시 만들었다.
        당일(8/30) 피처   변화 없음                                      ✔
        다음날(8/31)      lag24, lag_prev_op, prev_day_mean, prev_day_max 만 변함  ✔
      의도대로 과거 정보만 흘러간다.


==============================================================
 [4] 보강 효과 측정
==============================================================
  조건: LightGBM(objective=l1), 작업내역 D12 의 시간순 롤링 폴드 그대로
        (5 to 9월 각각 검증, 그 이전 전체 학습).
        평가셋은 모든 실험에서 동일하게 "고유일 & train_ok" 2,092행으로 고정.
  ※ 검증용 임시 측정이다. 스크립트는 저장소에 포함하지 않았다.
    모델 단계는 메인 개발자가 정식으로 구현하며, 아래 수치는 방향 확인용이다.
    LightGBM 의 bagging·feature_fraction 때문에 재실행 시 ±0.2 정도 흔들린다.

  ▷ 시나리오 1: 생산계획·기상을 받는 경우 (full, 36개 피처)
      구성                              MAE     RMSE   peakMAE
      보강 전                          11.13   22.09    17.98
      + 새 피처 4종                    11.10   21.09    17.83
      + 생산기록 누락일 제외(strict)   10.35   18.32    15.94
      → 개선 폭의 대부분은 "새 피처"가 아니라 "누락일 제외"에서 나왔다.

  ▷ 시나리오 2: 달력과 과거 전력만 있는 경우 (no_plan, 19개 피처)
      단계                                    MAE     RMSE   peakMAE
      ① 기존 (is_off 포함)                   13.36   25.30    21.08
      ② is_off 제거 (타깃 유래 정보 차단)    25.32   43.53    33.88
      ③ + 새 달력 피처 3종                   23.19   41.92    30.89
      ④ + 생산기록 누락일 제외               21.56   40.51    27.15
      → ① → ② 가 이번 보강에서 가장 중요한 변화다. 숫자는 나빠 보이지만
        ①이 쓸 수 없는 정보를 쓰고 있었다.

  ▷ 새 피처를 하나씩 빼봤을 때 (full + strict 기준, MAE 10.353에서 시작)
      days_since_off 제거   +1.001   ← 유일하게 뚜렷하다
      is_holiday     제거   +0.319
      day_prod_zero  제거   +0.208
      off_run_prev   제거   +0.084
      gain 중요도로는 넷 다 하위권이지만, 빼보면 days_since_off 는 확실히 기여한다.
      중요도가 여러 피처로 분산되기 때문이다. 판단은 제거 실험 쪽을 믿는 게 낫다.

  ▷ 베이스라인 대비 (작업내역 [9], 고유일 기준)
      목표선     lag168 MAE 23.6 / 요일x시간 RMSE 37.6 / peakMAE 37.1
      full+strict  MAE 10.35 / RMSE 18.32 / peakMAE 15.94  → 세 지표 모두 우위
      no_plan+strict MAE 21.56 / RMSE 40.51 / peakMAE 27.15 → MAE·peak 우위, RMSE 열위
      ※ full 쪽은 "당일 생산계획과 실측 기상을 그대로 받는다"는 가정 위의 값이다.
        실제 예보 오차만큼 낙관적이라는 점을 보고서에 반드시 명시할 것.


==============================================================
 [5] 데이터셋은 어떤 성격인가
==============================================================

  ▷ 규모와 구조
      2021-01-01 to 09-14, 시간 단위 6,168행 = 257일 x 24시간. 시간 누락 없음.
      컬럼 18개: 전력(15/30/45/60분·평균), 생산량, 공장인원, 인건비,
                 전기요금(계절), 기상 4종(기온·풍속·습도·강수량), 달력 3종(day/d/m)
      평균 = 15분 값 4개의 산술평균(반올림)임을 확인했다.
             따라서 15분 최대 >= 시간 평균이 항상 성립한다.

  ▷ 성격 1. 절반 이상이 증강 복제 데이터 ★ 가장 중요
      257일 중 161일(63%)은 24시간 전력곡선이 다른 날과 완전히 같다.
      1 to 6월에 몰려 있고 7 to 9월은 거의 전부 고유일이다.
      전력만 복사되고 생산량·기상은 복사되지 않아서, 복제일에는
      "생산은 많은데 전력은 평탄" 같은 모순된 행이 섞여 있다.
      → 랜덤 분할 금지(같은 곡선이 학습·검증에 동시 진입).
      → 실제 예측력은 고유일(7 to 9월) 성적으로만 판단한다.

  ▷ 성격 2. 전력이 연속값이라기보다 세 덩어리
      기저부하 약 22 (무가동, 공조·조명 등) / 중부하 약 105 / 고부하 약 165
      가동일 전력 분위수: 10% 23 · 25% 93 · 50% 112 · 75% 155 · 90% 171
      → 회귀 정확도보다 "가동 상태 전환"을 맞히는 게 성능을 가른다.

  ▷ 성격 3. 요일·시각 구조가 강하다
      평일 평균 103 to 123 / 토 51 / 일 41
      평일 8 to 11시, 13 to 19시 고부하, 12시 점심에 하락
      화 to 토 새벽 1 to 6시에도 약 90 (야간 가동), 월요일 새벽은 기저부하
      토요일은 새벽 1 to 6시만 가동하고 8시 이후 기저부하

  ▷ 성격 4. 품질 이슈가 여러 겹
      시간 컬럼 손상   7/13, 7/15 (48행)
      생산 기록 누락   15일 (보강 1에서 확대 탐지)
      가동 중단        20행 (8/28 17시 to 8/29 11시 + 9/8 12시). 휴무일에도 유지되는
                       기저부하 22 가 0이라 정전·계측 누락으로 본다
      결측             풍속 3 / 강수량 1 / 공장인원 17 (소량)
      휴무일           49일 (일요일 29 + 하계휴가 7/31 to 8/8 등)
      중복             날짜+시간 중복 없음

  ▷ 성격 5. 휴무일 경계가 뚜렷하다
      일 최대전력이 30 미만인 날의 최대값 26, 30 이상인 날의 최소값 97.
      26과 97 사이가 비어 있어 임계 30은 어디에 두어도 결과가 같다.

  ▷ 성격 6. 학습 구간에 없는 조건이 테스트에 나올 수 있다
      학습은 9/14까지다. 테스트가 9/15 이후라면
        - 추석(9/20 to 22): 학습 구간에 추석 사례가 없다
        - 9월은 전기요금 계절이 167.2로 바뀐다 (6 to 8월 191.6)
        - 9월 고유일은 14일뿐이라 가을 패턴 표본이 적다


==============================================================
 [6] 무엇과 무엇이 연결되어 있기에 전력을 예측할 수 있나
==============================================================
  ※ 아래 상관은 모두 스피어만(순위 상관), 고유일 + train_ok_strict 기준이다.
    복제일을 섞으면 관계가 흐려지므로 반드시 분리해서 본다.

  ----------------------------------------------------------------
  (1) 생산량 — 가장 강한 설명 변수
  ----------------------------------------------------------------
      생산량 ~ 전력   0.862      공장인원 ~ 전력  0.834
      ※ 생산량과 공장인원은 서로 0.99 로 사실상 같은 변수다. 둘 다 넣으면
        SHAP 중요도가 둘로 나뉘어 해석이 흐려진다. 하나만 쓰는 게 낫다.

      단 관계가 직선이 아니다. 생산량이 늘수록 전력도 늘지만 약 1,000 부근에서 포화된다.
        생산량 구간(고유일 가동시간)   평균 전력
          1 to 73                        104.2
          73 to 204                      106.6
          204 to 506                     117.5
          506 to 972                     149.7
          972 to 1,618                   162.2
          1,618 초과                     158.0   ← 더 만들어도 전력이 안 오른다
      → 그래서 prod_cap(800 상한)·prod_log 같은 포화 반영 피처가 원본 생산량보다
        상관이 높다 (prod_cap 0.867).
      → 피크 저감 관점에서 중요하다. 고부하 구간이 오히려 생산 단위당 전력이
        적으므로, 전체를 낮추기보다 맨 윗부분만 깎는 쪽이 유리하다 (D16).

  ----------------------------------------------------------------
  (2) 기온 — 가동 중일 때만, 그리고 생산량과 함께 작용한다
  ----------------------------------------------------------------
      전체 상관은 0.02 로 거의 없다. 가동·무가동이 섞여 효과가 가려지기 때문이다.
      생산량 구간을 고정하고 기온 구간별 평균 전력을 보면 관계가 드러난다.

        생산량 구간      ~22°C   22 to 26°C   26°C+     차이
        생산 0            47.0      37.9       40.4     없음
        저(1 to 300)     103.2     106.8      118.1     +14.9
        중(300 to 900)   118.9     137.6      163.2     +44.3
        고(900+)         144.3     159.2      171.6     +27.3

      해석
        - 생산 0(무가동)일 때는 기온 효과가 없다 → 건물 냉방이 아니라
          설비 가동에 수반되는 냉방으로 보인다.
        - 생산량이 많을수록 기온의 추가 효과가 크다 → 기온과 생산량은
          더해지는 게 아니라 곱해지는 관계다.
      → 팀원의 "22°C 이상에서 전력 상승" 결론은 확인됐고, 여기에
        "가동 중일 때만, 생산량과 상호작용" 조건이 붙는다.
      → cool(= max(0, 기온-22)) 을 단독으로 넣는 것보다 생산량과의
        상호작용을 모델이 잡도록 하는 편이 낫다. 트리 모델은 자동으로 잡지만,
        선형 모델을 쓸 경우 곱셈 항을 명시적으로 넣어야 한다.

  ----------------------------------------------------------------
  (3) 과거 전력 (lag) — 요일 주기가 하루 주기보다 강하다
  ----------------------------------------------------------------
      자기상관 (작업내역 [5] 기준)
        1시간 전 0.90 / 1일 전 0.37 / 1주 전 0.74
      → "어제 같은 시각"보다 "지난주 같은 요일·시각"이 훨씬 유효하다.
        공장이 요일 단위로 운영되기 때문이다.
      피처 상관
        lag_week_mean4 (최근 4주 같은 요일·시각 평균)  0.737
        lag336 (2주 전)                                0.731
        lag168 (1주 전)                                0.656
        lag24  (1일 전)                                0.535
      → 단일 lag168 보다 최근 4주 평균이 더 안정적이다. 노이즈가 줄기 때문.
      주의: lag 계열은 휴무·중단 값을 결측 처리하므로 결측률이 높다
            (lag168 21.6%, lag336 23.9%). lag_prev_op(직전 가동일 같은 시각)는
            휴무를 건너뛰어 참조하므로 결측률이 0.4% 로 낮다.
            8월 lag168 붕괴(MAE 42.9)는 휴가 다음 주가 휴가 기간을 참조해서 생긴 일이다.

  ----------------------------------------------------------------
  (4) 달력 — 생산계획을 대신하는 정보
  ----------------------------------------------------------------
      is_weekend ~ 전력  -0.569
      is_off     ~ 전력  -0.610   (단 타깃 유래. 생산계획이 있을 때만 사용)
      day_prod_zero      -0.610   (생산계획만으로 만드는 누수 없는 대체값)
      인건비             -0.234   (= 9 to 17시 여부를 뒤집은 값. 시각에서 그대로 나옴)
      전기요금(계절)     -0.032   (월로 완전히 결정됨 → month 와 중복, 제외 가능)
      풍속 0.116 / 강수량 0.082 / 습도 -0.073 → 모두 약하다

  ----------------------------------------------------------------
  (5) 그래서 왜 예측이 가능한가 — 한 문단 정리
  ----------------------------------------------------------------
      이 공장의 전력은 "얼마나 만드느냐"가 대부분을 결정하고(상관 0.86),
      거기에 "얼마나 더우냐"가 가동 중일 때만 얹힌다(생산량 구간 내 최대 +44).
      그리고 생산 자체가 요일 단위로 반복되기 때문에(1주 자기상관 0.74),
      생산계획을 모르더라도 지난주 같은 요일·시각 값으로 상당 부분을 복원할 수 있다.
      따라서 두 갈래가 성립한다.
        생산계획을 받으면 → 생산량 중심으로 예측 (MAE 약 10)
        달력만 있으면    → 지난주 같은 요일 패턴 중심으로 예측 (MAE 약 22)
      예측이 깨지는 지점은 이 규칙이 안 통하는 곳이다.
      즉 휴무 전후, 생산 0인데 전력을 쓰는 시간, 교대 전환 시각(7·12·17시)이며
      이 세 가지가 "예측오차가 큰 생산조건" 분석(과제 요구사항 ②)의 후보다.

  ----------------------------------------------------------------
  (6) 피크는 생산량만으로 설명되지 않는다
  ----------------------------------------------------------------
      피크 시 평균: 생산량 1,158 · 기온 20.6   (비피크 426 · 15.6)
      그런데 생산량 상위 10% 시간의 86.7% 는 피크가 아니다.
      → 생산량 단독이 아니라 설비 동시가동·기온이 겹치는 조합 조건이 피크를 만든다.
        서브 개발자의 "피크 위험조건" 분석은 단일 변수 임계가 아니라
        조합 규칙으로 접근해야 한다.
      요금 기준 피크는 15분 최대다(한국 최대수요전력 기준).
      시간 평균과의 차이는 평균 7.3이고 7시·17시·12시에 특히 크다.
      → outputs/peak_ratio.csv 의 시각별 계수로 환산한다.


==============================================================
 [7] 남은 이슈
==============================================================

  ▷ 즉시 확인 필요
    [!] 로컬 conda 환경(kamp-contest)의 numpy 2.4.6 이 깨져 있다.
        2x2 행렬 곱셈(@)만으로 인터프리터가 죽는다(0xc06d007f).
        이 때문에 eda.py 가 실행되지 않고 run_all.py 가 중간에 멈춘다.
        코드 문제가 아니다 — 원본 코드로 되돌려도 똑같이 죽는다.
        조치: 해당 환경에서 numpy 재설치. 확인 명령은
              python -c "import numpy as np; print(np.arange(4.).reshape(2,2) @ np.eye(2))"
    [!] environment.yml 에 버전 고정이 전혀 없다.
        그래서 팀원 환경과 로컬 환경의 numpy·pandas·matplotlib 버전이 갈렸다.
        재현성 배점(10점) 항목이므로 동작하는 조합으로 고정할 것.
        현재 로컬: python 3.11.16 / pandas 3.0.6 / numpy 2.4.6 / matplotlib 3.11.2

  ▷ 팀 합의 필요
    [v] train_ok vs train_ok_strict → strict 권고. 1차 실험에서 MAE +1.06 차이 ([9] ②)
    [v] 복제일 처리 → 가중치 0.3 권고. 제외는 확실히 나쁨 ([9] ①)
        ※ 위 둘은 측정 근거가 나왔을 뿐 팀 승인 전이다. 최종 확정은 팀 회의에서
    [ ] 피크 정의: 시간 평균 vs 15분 최대, 임계값 (D10 미결)
    [ ] 추석 휴무 여부 — 주최 측 확인. 이 공장은 공휴일에 가동하므로
        기본 가정은 "추석에도 가동"이 맞다 ([3] 보강 5)

  ▷ 주최 측 문의 (해둘 가치는 있으나, 답을 기다릴 필요는 없음)
    [ ] 테스트 데이터 형태 (구간, 생산량·기온 제공 여부)
        과제 원문(code_artifact.txt)에 별도 테스트 파일 언급이 없다. 데이터셋은
        okm_augumented_2021.csv 하나뿐이고 제출물에만 "테스트데이터 예측결과 파일"이 있다.
        → 주어진 CSV 뒷부분을 직접 홀드아웃으로 떼어 쓰는 방식일 가능성이 높다.
          그렇다면 그 구간의 생산량·기온도 CSV 안에 있으므로 full 세트를 쓸 수 있다.
        방침: full 을 주력으로 하고 no_plan 을 비교군으로 함께 보고한다 ([9] 3번).
              두 세트 비교 자체가 배점 1번(검증전략)·2번(모델 비교)에 해당하고,
              "생산계획을 하루 일찍 확정하면 예측 정확도가 두 배" 라는 현장 제언으로
              배점 4번(현장 활용방안)까지 이어진다.
    [v] 15분 컬럼이 15분 평균인지 순간값인지 → 두 해석 모두 구현·비교 완료 ([10])
        결론: 어느 쪽이든 결과가 98.7% 일치하므로 진행에 지장 없다

  ▷ 다음 작업
    [v] LightGBM·RandomForest 구현 → src/model.py ([8])
    [v] 피처 세트 비교 (full vs no_plan) → [9] ③
    [ ] run_all.py 에 모델 단계 추가 (실험 설정 확정 후에)
    [ ] 복제일 가중치 값 탐색 (0.1 / 0.3 / 0.5) — 0.3 은 임의로 정한 값이다
    [ ] 하이퍼파라미터 조정 (구조가 확정된 뒤에)
    [ ] 15분 최대 타깃 (환산 vs 별도 학습, D03)
    [ ] SHAP 영향요인 분석 (배점 15점)
    [ ] 예측오차 다발 조건 분석 — 휴무 전후 / 생산 0 고부하 / 교대 전환 시각


==============================================================
 [8] 모델 코드 설명 (src/model.py)
==============================================================
  실행: python src/model.py  →  outputs/model_results.csv  (약 110초)
  설정 6개 x 시드 3개 x 폴드 5개 = 90번 학습한다.

  ----------------------------------------------------------------
  8-1. 이 문제를 어떻게 푸는가 — 시계열이 아니라 표(table) 회귀다
  ----------------------------------------------------------------
      전력 예측이라 하면 ARIMA·LSTM 을 떠올리기 쉽지만, 이 문제는 지도학습 회귀로
      바꿔서 푼다. 핵심은 "과거 정보를 X 의 열로 집어넣는 것"이다.

        한 행 = 한 시간
        X = 그 시각을 예측할 때 이미 알고 있는 정보 36개 (lag168, 생산계획, 기온 ...)
        y = 그 시각의 전력 (target)

      예) 9/20 14시를 예측할 때, 일주일 전 9/13 14시의 전력값을 lag168 이라는
          열 하나로 넣는다. 그러면 시간 순서 개념이 사라지고 평범한 회귀가 된다.
      이 변환은 features.py 가 이미 끝냈다. model.py 는 그 결과를 받아 쓸 뿐이다.

  ----------------------------------------------------------------
  8-2. 함수 구성
  ----------------------------------------------------------------
      make_model(name, seed)
          모델 1종을 만들어 돌려준다. 현재 lgbm(LightGBM), rf(RandomForest).
          새 모델은 여기에만 추가하면 나머지 코드는 건드릴 필요가 없다.

      rolling_eval(X, cols, model, train_flag, clone, seed)   ★ 핵심
          시간순 롤링 폴드로 학습·예측한 결과를 행 단위로 돌려준다.
          여기만 정확하면 나머지는 자유롭게 바꿔도 안전하다.

      score(s)
          MAE / RMSE / peakMAE / n 계산. baseline.py 와 같은 정의라 직접 비교된다.

      run_all(X, runs, base, seeds)
          설정 여러 개를 시드별로 돌려 폴드별 + 합산(ALL) 성적표를 만든다.

  ----------------------------------------------------------------
  8-3. 검증 규약 — 왜 랜덤 분할을 쓰면 안 되는가
  ----------------------------------------------------------------
      보통 쓰는 train_test_split / KFold 를 이 데이터에 쓰면 점수가 크게 부풀려진다.
      이유가 두 가지다.
        1) 미래로 학습해서 과거를 맞히게 된다. 현실에서 불가능한 일이라 무의미하다.
        2) 복제일 161일 때문에 같은 전력곡선이 학습셋과 검증셋에 동시에 들어간다.
           모델이 답을 외운 것을 실력으로 착각하게 된다.
           (lag168 베이스라인이 복제일 MAE 11.6 vs 고유일 23.6 으로 2배 차이)

      그래서 시간순 롤링 폴드를 쓴다 (D12). baseline.py 와 완전히 같은 규약이다.
        5월 검증 <- 1 to 4월 학습 / 6월 검증 <- 1 to 5월 학습 / ... / 9월 검증 <- 1 to 8월 학습

      ★ 평가셋은 어떤 설정을 주어도 항상 "고유일 & train_ok" 로 고정된다.
        학습 조건을 바꿀 때마다 평가 기준까지 흔들리면 비교 자체가 성립하지 않는다.

  ----------------------------------------------------------------
  8-4. 실험 손잡이 4개
  ----------------------------------------------------------------
      BASE 를 기준으로 각 run 이 "한 가지만" 덮어쓰는 구조다.
      두 가지를 동시에 바꾸면 무엇 때문에 좋아졌는지 알 수 없다.

        cols        FEATURES 의 full (36개) / no_plan (19개)
        model       lgbm / rf
        train_flag  train_ok / train_ok_strict                      (D18)
        clone       keep 그대로 / drop 학습 제외 / weight 가중치 하향  (D07)

  ----------------------------------------------------------------
  8-5. 시드를 3개 돌리는 이유
  ----------------------------------------------------------------
      LightGBM 은 bagging·feature_fraction 때문에 같은 설정이어도 실행할 때마다
      결과가 조금씩 다르다. 한 번만 돌리면 우연히 좋게 나온 값을 실력으로 착각한다.
      그래서 SEEDS = (0, 1, 2) 로 세 번 돌려 평균과 표준편차(흔들림)를 함께 낸다.

      ★ 판단 기준: "기준대비" 차이가 "흔들림"보다 작으면 차이 없다고 본다.

  ----------------------------------------------------------------
  8-6. 함정: 폴드가 건너뛰어지면 비교가 성립하지 않는다
  ----------------------------------------------------------------
      학습 행이 200행 미만이면 그 폴드를 건너뛴다. 이때 평가 행 수(n)가 달라져
      다른 설정과 MAE 를 직접 비교할 수 없게 된다.
      실제로 복제일 학습 제외 설정에서 이 일이 일어났다 ([9] 1번 참고).
      코드가 이 경우를 자동으로 감지해 경고를 출력한다.


==============================================================
 [9] 1차 비교 실험 결과 (2026-09-23)
==============================================================
  조건: LightGBM/RandomForest, 시간순 롤링 폴드, 고유일 2,092행에서만 평가,
        시드 3개(0,1,2) 평균. 원자료는 outputs/model_results.csv

  [폴드 합산]
    설정                              MAE   흔들림  기준대비   RMSE  peakMAE     n
    0 기준 (lgbm·full·strict·keep)   10.41   0.33    0.00   18.36   16.42   2092
    1 복제일 학습 제외                13.81   0.23    3.41   22.87   24.17   1756
    2 복제일 가중치 0.3                9.57   0.39   -0.84   17.45   16.12   2092
    3 누락일 포함 (train_ok)          11.47   0.33    1.06   21.38   18.14   2092
    4 피처 no_plan                   21.16   0.23   10.76   39.76   28.08   2092
    5 RandomForest                  10.81   0.11    0.40   19.87   21.30   2092

  [폴드별 MAE]
    설정                              5월    6월    7월    8월    9월
    0 기준                           18.1   1.2   11.5    8.7    5.5
    1 복제일 학습 제외                 NaN   8.5   11.7   19.8    5.8
    2 복제일 가중치 0.3               19.6   1.2    9.9    7.2    5.2
    3 누락일 포함 (train_ok)          20.7   1.1   10.1   12.0    5.2
    4 피처 no_plan                   30.3   1.1   14.0   32.0    5.2
    5 RandomForest                  16.5   1.0   13.9    8.7    5.2
    ※ 6월은 고유일이 2일뿐이라 수치를 해석하지 말 것

  ----------------------------------------------------------------
  1. 복제일 처리 (D07) → 가중치 0.3 채택 권고
  ----------------------------------------------------------------
      가중치 0.3   MAE 9.57 (기준대비 -0.84, 흔들림 0.39)
                   차이가 흔들림의 2배 이상이라 우연이 아니다. RMSE·peakMAE 도 전부 우위.
      학습 제외    MAE 13.81 이지만 n=1756 이라 직접 비교 불가.
                   1 to 4월이 거의 전부 복제일이라, 제외하면 5월 폴드의 학습 데이터가
                   200행 미만이 되어 폴드가 통째로 사라졌다.
                   공통 폴드(6 to 9월)끼리만 비교하면 결론은 더 뚜렷하다.
                     6월  1.2 -> 8.5
                     8월  8.7 -> 19.8
                     7·9월은 비슷
                   → 복제일을 버리면 확실히 나빠진다.

      해석
        복제일 161일을 버리는 손해가, 복제로 인한 왜곡보다 크다.
        전력곡선은 복사됐지만 생산량·기상은 실측이므로 "버릴 데이터"가 아니라
        "덜 믿을 데이터"로 다루는 게 맞다. 가중치 하향이 정확히 그 처리다.
      남은 일
        CLONE_W = 0.3 은 임의로 정한 값이다. 0.1 / 0.3 / 0.5 를 비교해 볼 것.

  ----------------------------------------------------------------
  2. 생산기록 누락일 제외 (D18) → strict 유지
  ----------------------------------------------------------------
      train_ok 로 되돌리면 MAE +1.06 (흔들림 0.33 의 3배), RMSE +3.02, peakMAE +1.72.
      세 지표가 모두 같은 방향이라 판단이 명확하다.
      특히 8월이 8.7 -> 12.0 으로 벌어진다.
      → 이 문서 [12] 의 D18 제안을 뒷받침하는 근거다.

  ----------------------------------------------------------------
  3. 피처 세트 → 테스트 데이터 형태 확인이 최우선 과제
  ----------------------------------------------------------------
      full 10.41 vs no_plan 21.16. 두 배 차이다.
      폴드별로 보면 격차가 특정 월에 몰려 있다.
        5월  18.1 -> 30.3
        8월   8.7 -> 32.0   <- 하계휴가(7/31 to 8/8) 구간
        9월   5.5 ->  5.2   <- 차이 없음
      해석
        평상시에는 지난주 같은 요일 패턴만으로도 충분히 맞힌다(9월).
        그러나 휴무처럼 패턴이 깨지는 구간에서는 생산계획이 없으면 무너진다(8월).
      → 주최 측에 "테스트에 생산량·기온이 제공되는가"를 묻는 것이 가장 시급하다.
        답에 따라 준비 방향이 완전히 달라진다.
      → 다만 no_plan(21.16)도 베이스라인(23.6)은 이긴다. 최악의 경우에도 쓸 수 있다.

  ----------------------------------------------------------------
  4. 모델 선택 → LightGBM 채택, RandomForest 는 비교군으로 남김
  ----------------------------------------------------------------
      MAE 만 보면 10.81 vs 10.41 (차이 0.40, 흔들림 0.11 to 0.33)로 경계선이라
      "조금 낫다" 정도로만 말할 수 있다.
      그런데 피크에서 갈린다.
        peakMAE  16.42 (lgbm) vs 21.30 (rf)   차이 4.88
        RMSE     18.36 (lgbm) vs 19.87 (rf)
      → 평균 오차는 비슷하지만 RandomForest 는 피크를 훨씬 못 맞힌다.
        이 과제는 최대피크 분석이 핵심이므로 LightGBM 을 쓴다.
      → RandomForest 는 "모델 2종 이상 비교" 요건을 채우는 비교군으로 남긴다.
        버리지 말고 보고서에 근거와 함께 기록할 것.

  ----------------------------------------------------------------
  5. 폴드 공통 경향
  ----------------------------------------------------------------
      5월이 모든 설정에서 가장 나쁘다 (16.5 to 30.3). 학습 데이터가 4개월치뿐이라
        그렇다. 데이터가 쌓일수록 좋아지는 정상적인 모습이다.
      9월이 가장 좋다 (5.2 to 5.8). 설정을 바꿔도 거의 변하지 않는다.
      8월은 설정에 가장 민감하다 (7.2 to 32.0). 하계휴가 때문이다.
        → 휴무 구간 처리가 성능을 가르는 지점이라는 것을 다시 확인했다.

  ----------------------------------------------------------------
  6. 종합
  ----------------------------------------------------------------
      현재 최적 조합
        lgbm · full · train_ok_strict · clone=weight(0.3)
        MAE 9.57 / RMSE 17.45 / peakMAE 16.12

      베이스라인(작업내역 [9] 고유일) 대비
        MAE      23.6 -> 9.57   (-59%)
        RMSE     37.6 -> 17.45  (-54%)
        peakMAE  37.1 -> 16.12  (-57%)
      세 지표 모두 목표선을 크게 넘었다.

      ※ 주의: full 세트는 "당일 생산계획과 실측 기상을 그대로 받는다"는 가정 위의
        값이다. 실제 기상은 예보라 오차가 있으므로 이 수치는 낙관적이다.
        보고서에 반드시 명시할 것.
      ※ 아직 하이퍼파라미터를 전혀 건드리지 않은 기본값 상태다.


==============================================================
 [10] 15분 컬럼의 의미 — 두 해석 비교 (2026-09-25)
==============================================================
  미결이던 "15분 컬럼이 15분 평균인가 순간값인가"를 양쪽으로 모두 구현해 비교했다.
  결론부터: 어느 쪽으로 가정해도 결과가 거의 같아서, 이 미결 때문에 막힐 일은 없다.

  ----------------------------------------------------------------
  10-1. 두 해석
  ----------------------------------------------------------------
      A안  네 값이 각 15분 구간의 평균 수요
           15분=[0 to 15분 평균] 30분=[15 to 30분 평균] 45분=[...] 60분=[...]
           → 한국 최대수요전력 정의와 정확히 일치. 네 값 중 최대가 곧 요금 기준 피크
           → target_max15

      B안  네 값이 :15 :30 :45 :60 시점의 순간값
           → 순간값 자체는 요금 기준이 아니므로 구간 평균을 추정해야 한다
           → 인접 두 값의 평균(사다리꼴). 첫 구간은 직전 시간의 60분 값을 0분 시점으로 씀
           → target_max15_trapz

  ----------------------------------------------------------------
  10-2. 어느 쪽이 데이터와 맞나 — 평균 컬럼 역산
  ----------------------------------------------------------------
      평균 컬럼을 각 해석의 공식으로 다시 만들어 원본과 대조했다.

        단순평균 (A안 전제)    절대오차 0.245   ±0.5 이내 100.0%   ← 반올림 오차 수준
        사다리꼴 (B안 전제)    절대오차 2.172   ±0.5 이내  30.6%

      → 평균 컬럼은 네 값의 단순 산술평균으로 만들어진 것이 확실하다.
        순간값이었다면 단순평균은 시간 평균의 편향된 추정치가 되므로(양 끝 가중이 틀림),
        데이터 작성자가 그렇게 만들지는 않았을 것이다. A안 쪽 정황이 강하다.
      ※ 다만 이것은 "평균 컬럼이 어떤 공식으로 만들어졌나"를 본 것이지
        15분 값의 물리적 의미를 증명한 것은 아니다. 단정하지 말 것.

  ----------------------------------------------------------------
  10-3. 실무적으로 더 중요한 것 — 두 해석의 차이가 작다
  ----------------------------------------------------------------
      두 값의 순위상관              0.9907
      피크 판정 일치율 (상위 5%)     98.7%   (A 247개 / B 258개 / 공통 221개)
      추정값 차이                   평균 0.59  (전력 평균 93.4 의 0.6%)

      시각별 환산 계수 (시간 평균 -> 15분 최대)
        시각      0      5      7     12     17    나머지
        A안    1.050  1.151  1.297  1.149  1.173   약 1.05
        B안    1.137  1.104  1.244  1.303  1.130   약 1.05
        차이  -0.087  0.046  0.054 -0.154  0.043   0.01 내외

      → 0시와 12시만 주의하면 된다.
        0시는 B안이 전날 23시 값을 끌어와 날짜 경계에서 튀고,
        12시는 점심 급변 구간이라 사다리꼴 추정이 민감하다.

  ----------------------------------------------------------------
  10-4. 처리 방침
  ----------------------------------------------------------------
      구현    features.py 가 두 타깃을 모두 만든다.
                target_max15        A안 (평균 100.7)
                target_max15_trapz  B안 (평균 100.2)
              outputs/peak_ratio.csv 에 ratio_a, ratio_b 두 열을 모두 낸다.
      방침    A안을 기본으로 진행하고 B안은 민감도 분석용으로 남긴다.
              보고서에 "두 해석을 모두 검토했고 결과가 98.7% 일치한다"고 쓰면
              데이터 진단 점수(배점 1번)에 유리하다.


==============================================================
 [11] 1주 앞 예측 (src/forecast.py) 와 뜻밖의 발견 (2026-09-25)
==============================================================

  ----------------------------------------------------------------
  11-1. 무엇이 달라지는가
  ----------------------------------------------------------------
      D01·D02 는 24시간 앞(전날 24시 마감) 전제다. 1주 앞으로 늘리면
      예측 시점이 대상일 7일 전이 되어, 그 사이 6일치 정보가 전부 "아직 미래"가 된다.
      기존 피처를 그대로 쓰면 누수다.

      예) 9/14(화)를 9/7(화)에 예측한다면
        쓸 수 있다   9/7 이전 전력 실적, 달력, (계획을 받으면) 9/14 생산계획·기상예보
        쓸 수 없다   9/13(월) 전력 = lag24. 아직 일어나지 않은 날이다

      피처 세트 h7_full / h7_no_plan 을 새로 만들었다 (D21).
        제외  lag24, lag_prev_op, prev_day_mean, prev_day_max   (전날 실적 = 미래)
        유지  lag168, lag336, lag_week_mean4, last_week_day_mean/max
              → 전부 7일 이상 과거라 1주 앞 시점에도 이미 알고 있다
        이동  after_off, off_run_prev, days_since_off 는 대상일 직전 휴무 정보라
              1주 앞 시점에는 생산계획이 있어야 안다 → 달력군에서 계획군으로

  ----------------------------------------------------------------
  11-2. ★ 뜻밖의 결과 — 1주 앞이 하루 앞보다 정확하다
  ----------------------------------------------------------------
      같은 조건(clone=weight, strict, 고유일 평가)에서 비교했다.

        설정                    MAE   흔들림   RMSE  peakMAE
        하루 앞  full          9.57   0.39   17.45   16.12
        1주 앞   h7_full       8.77   0.09   16.33   16.52
        하루 앞  no_plan      20.75   0.41   39.38   26.15
        1주 앞   h7_no_plan   17.91   0.09   36.54   25.86

      예측 시계를 7배 늘렸는데 오차가 줄었다. 흔들림도 0.39 → 0.09 로 안정됐다.
      두 세트는 피처 4개 차이뿐이므로(h7_full = full - 4개) 원인이 명확하다.

      하나씩 빼본 결과 (full 기준 MAE 9.566)
        -lag24           9.256  (-0.310)
        -lag_prev_op     9.401  (-0.164)
        -prev_day_mean   9.450  (-0.115)
        -prev_day_max    9.079  (-0.486)
        -4개 모두        8.767  (-0.799)
      → 네 개 전부 개별적으로도 해가 된다.

  ----------------------------------------------------------------
  11-3. 왜 그런가 — 요일 전환 지점에서 lag24 가 오도한다
  ----------------------------------------------------------------
      이 공장은 요일 단위로 운영된다(1주 자기상관 0.74 vs 1일 0.37).
      그래서 "어제"는 대체로 다른 성격의 날이다.

      요일별 평균 오차 (고유일)
        요일    lag24    lag168
        월       결측     15.6      <- 어제가 일요일(휴무)이라 lag24 자체가 없음
        화       31.9     22.1
        수       22.4     42.5
        목       15.7     24.4
        금       13.2     18.4
        토       82.8     12.4      <- 금요일 고부하를 토요일에 그대로 갖다 댄다
        일       38.8      7.3

      토·일에서 lag24 오차가 lag168 의 5 to 7배다. 평일→주말 전환에서
      "어제 값"이 완전히 다른 운영 상태를 가리키기 때문이다.
      게다가 휴무·중단 값을 결측 처리하므로 lag24 는 결측률도 19.8% 로 높다.

  ----------------------------------------------------------------
  11-4. 그래서 무엇을 할 것인가
  ----------------------------------------------------------------
      ★ 하루 앞 예측에서도 lag24 계열을 빼는 것이 낫다.
        h7_full 은 이름만 "1주 앞"일 뿐, 실제로는 더 나은 기본 피처 세트다.
        D01(24시간 앞) 전제를 유지하더라도 이 네 개는 제외하는 쪽으로 검토할 것.
      ※ 단 peakMAE 는 16.12 → 16.52 로 아주 조금 나빠진다. 피크만 보면 호각이다.
        평가 지표 우선순위(MAE vs peakMAE)가 정해지면 다시 판단할 것.
      ※ 예측 시계를 늘려도 성능이 유지된다는 것은 보고서에 쓸 만한 내용이다.
        "일주일 전에 미리 알려줘도 정확도가 떨어지지 않는다"는 현장 활용 근거가 된다
        (배점 4번). 생산일정을 조정할 시간이 하루가 아니라 일주일 생긴다.

  ----------------------------------------------------------------
  11-5. forecast.py 사용법
  ----------------------------------------------------------------
      python src/forecast.py            마지막 예측 가능일을 자동 선택
      python src/forecast.py 20210914   날짜 지정

      동작
        1) 대상일 7일 전 0시를 예측 시점(cutoff)으로 잡는다
        2) cutoff 이전 행만으로 학습한다 (train_ok_strict, clone=weight)
        3) 대상일 24시간의 시간 평균 전력을 예측한다
        4) 시각별 계수(peak_ratio)로 15분 최대를 환산한다
           ※ 계수도 학습 구간에서만 뽑는다. 대상일 정보를 쓰면 누수다
        5) outputs/forecast_{날짜}.csv 로 저장한다

      출력 열  dt, 날짜, hour, pred, pred_max15, actual, actual_max15
      실적이 있는 날이면 MAE·RMSE·일 피크 비교까지 출력한다.

      시험 실행 (20210914, 학습 5,597행)
        MAE 5.88 · RMSE 8.30 · 최대오차 24.6
        일 피크  예측 10시 177 (15분 환산 190) / 실제 8시 174 (15분 최대 184)
        → 피크 크기는 잘 맞췄으나 시각을 2시간 빗맞혔다.
          피크 시각 정확도는 별도 지표로 관리할 필요가 있다.

      한계
        - 데이터에 존재하는 날짜만 예측한다. 9/15 이후를 예측하려면
          그날의 생산계획·기상예보 행을 먼저 데이터에 넣어야 한다.
        - 하루치만 예측한다. 여러 날을 한 번에 예측하려면 반복 호출하거나
          기간 인자를 받도록 확장할 것.


==============================================================
 [12] 팀 공용 문서에 반영할 내용 (제안)
==============================================================
  작업내역.txt · 피처목록.txt · README.md 는 팀원이 관리하는 문서라 직접 고치지 않았다.
  아래는 이번 작업으로 바뀐 사실들이라 언젠가는 반영되어야 한다.
  팀 회의에서 합의된 항목만 골라 그쪽 문서에 옮기면 된다.
  ※ 반영 전까지는 작업내역.txt 의 D01 to D16 과 피처목록.txt 가 옛 상태다.
    코드 주석은 D17 이후 번호 대신 이 문서의 절 번호를 가리키도록 해 두었다.

  ----------------------------------------------------------------
  12-1. 작업내역.txt — 결정사항에 추가 제안 (번호는 임시)
  ----------------------------------------------------------------
  D17 [제안] is_off 는 A군(달력)이 아니라 B군(생산계획) 피처다
      이유     is_off 는 "일 최대전력 30 미만"으로 타깃에서 유도한 값이다. D09 가정대로
               생산계획으로 알 수 있다 치더라도, 생산계획이 없는 no_plan 세트에
               들어가 있으면 쓸 수 없는 정보를 쓰는 셈이 된다
      근거     제거 후 no_plan 실측 MAE 13.36 → 25.32 (기존 값이 부풀려져 있었음)
      세부     "지난 휴무 이력"(after_off, off_run_prev, days_since_off)은 전날 실적으로
               알 수 있으므로 A군에 남긴다. 당일을 보는 값과 과거를 보는 값을 가른 것
      영향     시나리오 2의 기대 성능을 MAE 약 22로 재설정해야 함
      상세     이 문서 [3] 보강 2

  D18 [제안] 생산 기록 누락 의심일 15일을 학습에서 제외 (train_ok_strict)
      정의     하루 종일 생산량 0인데 일 최대전력이 기저부하를 크게 넘는 날 (is_prod_missing)
               20210101 0102 0110 0124 0211 0212 0213 0301 0307 0314 0321 0328 0509 0713 0715
      이유     D14의 7/13, 7/15는 시간 컬럼까지 손상돼 우연히 발견된 것이고 전체 목록이
               아니었다. 예) 3/21(일) 생산 0인데 일 최대 190
      근거     생산량-전력 상관 0.825 → 0.862 / LightGBM MAE 11.10 → 10.35 (full, 고유일)
      상태     기존 train_ok 는 그대로 두고 train_ok_strict 를 따로 뒀다. 무엇을 기본으로
               쓸지는 팀 합의 필요. 복제일 9일이 섞여 있어 D07 결정에 따라 재검토
      상세     이 문서 [3] 보강 1, [9] 2번

  D19 [제안] 이 공장은 법정공휴일에 쉬지 않는다 → 추석에도 가동을 기본 가정으로 둔다
      근거     학습 구간 공휴일 10일 중 실제 휴무는 6/6, 8/15 둘뿐이고 둘 다 일요일이다.
               5/5 생산 2,190 / 5/19 생산 2,990 / 8/16 대체공휴일 생산 10,238 으로 정상 가동
      영향     D09 의 "테스트 구간에 추석 포함 가능성 → 모델이 휴무를 배워야 함"은 전제 수정.
               실제 조업 중단은 하계휴가(7/31 to 8/8) 같은 회사 자체 휴무로만 나타난다
      세부     is_holiday 피처를 넣어는 뒀으나 기여도 하위권(36개 중 30위).
               주최 측이 추석 휴무를 알려주면 피처가 아니라 is_off 를 직접 지정하는 게 안전
      상세     이 문서 [3] 보강 5

  D20 [제안] 15분 컬럼 해석은 A안(각 15분 구간의 평균)을 기본으로 하고
             B안(순간값)은 민감도 분석용으로 함께 구현해 둔다
      근거     ① 평균 컬럼을 역산하면 단순 산술평균과 절대오차 0.245(100%가 ±0.5 이내),
                 사다리꼴(B안 전제)은 2.172(30.6%만 일치) → A안 정황이 강함
               ② 두 해석의 결과 차이가 작다. 순위상관 0.9907, 피크 판정 일치율 98.7%
      주의     0시·12시만 환산 계수 차이가 크다(0.09, 0.15). 나머지는 0.05 미만
      → D03 의 "15분 컬럼 의미 확인 결과" 대기 항목과 D10(피크 정의)에 영향
      상세     이 문서 [10]

  D21 [제안] 예측 시계(horizon)별로 피처 세트를 나눈다
      이유     D01·D02 는 24시간 앞 전제라 lag24·전날 통계를 쓴다. 1주 앞 예측에서는
               그 값들이 예측 시점에 존재하지 않아 그대로 쓰면 누수다
      구현     FEATURES 에 h7_full / h7_no_plan 추가
      ★주의    h7 세트가 full 보다 정확하다 (MAE 9.57 → 8.77). lag24 계열이 요일 전환에서
               크게 오도하기 때문. 하루 앞 예측에서도 이 넷을 빼는 것을 검토해야 한다
      상세     이 문서 [11]

  ▷ 작업내역.txt 의 다른 절에도 반영이 필요한 것
    [3] 데이터 품질 이슈 표
        "생산 기록 누락  7/13, 7/15" → 15일로 확대 (is_prod_missing)
        학습·평가 행 수  6,100행(train_ok) 외에 5,788행(train_ok_strict) 추가
    [5] 전력 패턴
        "일요일 외에 1월 화·수, 3월 금요일에도 평일 무가동 (모두 복제일)" 은 그대로 맞다.
        여기에 "휴무 49일은 전부 일 생산량 0" 을 덧붙이면 좋다
    [9] 검증 전략
        피크 임계는 여전히 시간 평균 상위 5% 임시 기준이다 (D10 미결)
    [10] 코드·산출물 현황
        아래 12-3 참고
    "정리 필요" 항목
        environment.yml 에 버전 고정이 없어 팀원 간 환경이 갈린다 (재현성 배점)

  ----------------------------------------------------------------
  12-2. 피처목록.txt — 변경 제안
  ----------------------------------------------------------------
  ▷ A군(달력)에서 빼야 할 것
      휴무일 is_off → B군으로 이동 (D17 제안). 타깃 유래 값이라 달력군에 있으면 안 된다

  ▷ A군에 추가된 것
      직전 연속 휴무 길이 off_run_prev    복귀일 일 최대 165(평상)/182(1일)/193(9일)   중
      마지막 휴무 이후 경과일 days_since_off  제거 실험 MAE +1.00 으로 새 피처 중 최대  중
      공휴일 is_holiday                  근거 약함. 공휴일 10일 중 8일 정상 가동      하
                                         (실제 휴무 2일은 둘 다 일요일) → 추석 보험용
      ※ 이 셋은 "지난 휴무 이력"이라 전날 실적으로 알 수 있어 A군에 남는다.
        단 1주 앞 예측에서는 계획군으로 옮겨야 한다 (D21 제안)

  ▷ B군(생산계획)에 추가된 것
      휴무일 is_off                      A군에서 이동. 생산계획 받는 시나리오에서만 사용  상
      일 생산계획 0 여부 day_prod_zero    휴무 49일이 전부 일 생산량 0 (포함관계).        중
                                         is_off 의 누수 없는 대체값, 일치율 93.75%
                                         (생산 기록 누락 15일 제외 시 100%)
      ※ "시간당 생산량 전력과 상관 0.79" → strict 기준으로는 0.86

  ▷ E군(쓰면 안 되는 것)에 추가
      is_off (no_plan 세트에서만)   타깃 유래라 생산계획 없이는 알 수 없다. full 에서는
                                    D09 가정에 따라 사용 가능하지만 no_plan 에 넣으면
                                    점수가 부풀려진다 (MAE 13.36 → 실제 25.32)
      생산 0인데 고부하인 날 15일    "생산 0인데 고부하"를 학습하면 생산량 피처 기울기가
        (is_prod_missing)           눌린다 → train_ok_strict 로 제외 권장 (11.10 → 10.35)

  ▷ H절(구현) 갱신 — 현재 코드 기준
      세트 이름  FEATURES['full']       (36개) = A + B + C + D      하루 앞
                 FEATURES['no_plan']    (19개) = A + D
                 FEATURES['h7_full']    (32개) = A7 + B7 + C + D7   1주 앞
                 FEATURES['h7_no_plan'] (12개) = A7 + D7
      열 이름  A   hour, dow, is_weekend, is_holiday, after_off, off_run_prev,
                   days_since_off, shift(0 야간/1 점심/2 주간), is_transition, month
               B   is_off, day_prod_zero, prod, prod_cap(800 상한), prod_log, prod_zero,
                   day_prod, day_prod_hours, prod_prev, prod_next, prod_diff
               C   cool(= max(0, 기온 - 22)), temp, humid, thi, wind, rain
               D   lag24, lag168, lag336, lag_week_mean4, lag_prev_op,
                   prev_day_mean/max, last_week_day_mean/max
               A7  A 에서 after_off, off_run_prev, days_since_off 를 뺀 것
               B7  B + after_off, off_run_prev, days_since_off
               D7  lag168, lag336, lag_week_mean4, last_week_day_mean/max
      메타 열  dt, 날짜, target, target_max15, target_max15_trapz, is_clone, is_off,
               is_stop, is_corrupt, is_prod_missing, train_ok, train_ok_strict
      부가 출력 outputs/peak_ratio.csv  시각별 환산 계수 (ratio_a = A안, ratio_b = B안)
      누수 검증 8/30(가동일) 전력을 바꿔도 당일 피처는 불변,
               다음날 lag24·lag_prev_op·prev_day_mean/max 만 변함 — 재검증 완료

  ▷ F절(타깃) 갱신
      15분 최대에 두 해석이 있다 → target_max15(A안) / target_max15_trapz(B안)
      환산 계수는 outputs/peak_ratio.csv 참조 (7시 1.30 · 17시 1.17 · 나머지 약 1.05)

  ----------------------------------------------------------------
  12-3. README.md — 변경 제안
  ----------------------------------------------------------------
  ▷ 폴더 구조에 추가
      src/model.py            예측 모델 학습·비교
      src/forecast.py         특정 날짜 1주 앞 예측
      outputs/peak_ratio.csv  시각별 15분최대/시간평균 환산 계수
      outputs/model_results.csv
      outputs/forecast_{날짜}.csv

  ▷ "문제 해결" 절 신설 제안
      eda.py 단계에서 오류 메시지 없이 프로세스가 죽으면 numpy 설치가 깨진 것이다.
        확인:  python -c "import numpy as np; print(np.eye(2) @ np.eye(2))"
        해결:  conda install -n kamp-contest --force-reinstall numpy
      코드 문제가 아니다. 원본 코드로 되돌려도 똑같이 죽는다.

  ▷ environment.yml
      버전 고정이 전혀 없어 팀원마다 numpy·pandas·matplotlib 버전이 갈린다.
      재현성 배점(10점) 항목이므로 동작하는 조합으로 고정할 것.
      현재 로컬: python 3.11.16 / pandas 3.0.6 / numpy 2.4.6 / matplotlib 3.11.2 (numpy 깨짐)

  ----------------------------------------------------------------
  12-4. 이 문서에서만 관리하는 것 (팀 문서에 옮기지 않아도 되는 것)
  ----------------------------------------------------------------
      · 실험 원자료와 중간 측정값 ([4], [9], [11])
      · 아직 팀 합의 전인 권고 (복제일 가중치 0.3, train_ok_strict 기본 채택 등)
      · forecast.py 사용법과 한계 ([11] 5)
      · 15분 컬럼 두 해석의 상세 비교 ([10])
      팀 문서에는 "무엇으로 정했는가"만 올리고, "어떻게 확인했는가"는 여기에 남긴다.
